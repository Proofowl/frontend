/**
 * Canonical identifier hashing (identifier-spec-v1). See ./identifiers.ts
 * for why this is a port of proofowl-backend's from-scratch
 * implementation rather than a re-export of the contracts SDK.
 */
export * from "./identifiers";
